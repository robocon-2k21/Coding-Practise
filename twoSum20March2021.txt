def twoSum(array, target):
	i = 0
        for i, num1 in enumerate(array):
            num2 = target - num1
            if num2 in array:
                return 1
            return 0
a = [2, 4, 5, 8]
print(twoSum(a, 9))
