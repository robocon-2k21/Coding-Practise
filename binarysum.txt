#include<stdio.h>
#include<conio.h>
#include<math.h>
#include<string.h>
void main()
{
 clrscr();
 int  deca=0,b,decb=0,i=0,rem,a,str[10];
 printf("Enter first binary number = ");
 scanf("%d",&a);
 printf("Enter second binary number = ");
 scanf("%d",&b);
   while(a>0||b>0)
       {
            deca+= (a%10 * pow(2,i));
           decb+= (b%10 * pow(2,i));
           a/=10; b/=10;
           i++;
      }
      int sum=deca+decb;
      for(i=0;sum>0;i++)
          {
                str[i]=sum%2;
                sum/=2;
          }
       printf("Sum of Binary = ");
      for(i=i-1;i>=0;i--)
         {
            printf("%d",str[i]);
         }
getch();
}