#include<stdio.h>
#include<conio.h>
int main()
{
int i,j,num[10],sum,target,size;
printf("Enter size of array= ");
scanf("%d",&size);
printf("Enter elements of array= ");
for( i=0;i<size;i++)
{
      scanf("%d",&num[i]);
}
      printf("Enter target= ");
      scanf("%d",&target);
      for( i=0;i<size;i++)
        { 
            for ( j=i+1;j<size;j++)
            {
                sum =num[i]+num[j];         
           }
        }
       if(sum==target)    printf("True");
      else   printf("False");
}