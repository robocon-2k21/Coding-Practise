#include <stdio.h>
#include <conio.h>
int main()
{   int v,l=0,r,c,d;
    printf("give value");
    scanf("%d", &v);
    r=v;
    while(l<=r){
        c=l+(r-l)/2;
        d=c*(c+1)/2;
        if(d==v)
        printf("%d", c);
        if(v<d){
            r=c-1;
        }else{
            l=c+1;
        }
    }
    printf("%d", r);

    return 0;
}
